---
import Layout from "./Layout.astro";
import Jumbotron from '../components/jumbotron.svelte';

const links = {
  getStarted: import.meta.env.PUBLIC_GITHUB_URL,
  learnMore: import.meta.env.PUBLIC_GITHUB_URL,
};

export interface Props {
  frontmatter: {
    title?: string;
    subtitle?: string;
    showButtons?: boolean;
  };
}

const { frontmatter } = Astro.props;
---

<Layout title={frontmatter?.title || "ETGraph"}>
  <Jumbotron {frontmatter} {links} client:load />
  <main
    class="flex-1 flex flex-col px-4 sm:px-6 lg:px-16"
    style="background-color: #E6E1D7"
  >
    <div
      class="flex-1 mx-auto max-w-4xl py-8 lg:py-16 prose prose-lg dark:prose-invert dark:prose-headings:text-slate-300 prose-md prose-headings:font-heading prose-headings:leading-tighter prose-headings:tracking-tighter prose-headings:font-bold prose-a:text-blue-600 dark:prose-a:text-blue-400 prose-img:rounded-md prose-img:shadow-lg"
    >
      <slot />
    </div>
  </main>
</Layout>
